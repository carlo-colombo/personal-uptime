{
  "name": "personal-uptime",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "@types/node": "^20.3.3",
    "typescript": "^5.1.6",
    "wrangler": "^4.51.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "migrate:local": "wrangler d1 execute personal-uptime --local --file=schema.sql && wrangler d1 migrations apply personal-uptime --local",
    "migrate": "wrangler d1 execute personal-uptime --file=schema.sql && wrangler d1 migrations apply personal-uptime"
  }
}
